# Metal Validator Toolkit

A grab-bag of small Bash and Python scripts we use to operate our Metal Blockchain validator (mainnet `NodeID-yyPvtQHTA4FZU5cJtjWZa7RVBpWU3i5v`). Everything in `scripts/` is committed to this public repo so other Metal validators — can fork what's useful, ignore what isn't.

There's no installer, no Docker image, no SaaS. Just shell scripts that the operator runs on their own node. Some are turnkey; some assume our specific two-host topology and need adaptation. The list below is honest about which is which.

## What this is not

- **Not a product**. No SLA, no support, no roadmap.
- **Not curated**. Anything the operator writes for their own node lives here. If a script is useful to others, it's a side benefit.
- **Not opinionated about your stack**. Bash + jq + cron + ntfy is what we use because it's boring and survives. Use whatever else suits you.
- **Not the operator's private bootstrap tooling**. Most scripts under `scripts/operator-local/` (identity generation and key-handling steps run only on the operator's own machine) are intentionally **excluded from this catalog** — they assume our specific keys and run order and are not reusable. One exception: `scripts/operator-local/commit-anchor-source.sh` (below, in the anchor pipeline table) touches no key material and is pipeline-critical git-transfer glue, not bootstrap tooling. This catalog covers the operational `scripts/` that other validators can fork.

## What problem this solves

Running a Metal validator is mostly waiting. The hard part is noticing when something quietly stops working — a stuck push pipeline, a missed renewal window, slow peer count drift, a fee-market shift you didn't catch. These scripts mechanize the noticing.

For the data side, we also publish daily-sampled time-series of the wider Metal validator set (gini, fee market percentiles, peer turnover, full daily snapshots) at [`/data/`](https://metal.freedom-yield.com/data/). Those JSON / JSONL feeds are generated by the same scripts below.

---

## Tier 1 — Core reusable scripts

Pure chain-level operations. Point them at your local `metalgo` HTTP API and your NodeID, and they work. No assumptions about your hosting topology.

### `scripts/node-info.sh`
**Purpose:** Query the running `metalgo` HTTP API and write a human-readable `public/api/validator.json` (NodeID, bootstrap, uptime, stake, delegation fee, period bounds, total network validator count).
**Dependencies:** `curl`, `jq`, `bc`. A running `metalgo` reachable at `${METALGO_API:-http://localhost:9650}`.
**Env:** `METALGO_API` (default `http://localhost:9650`), `OWN_NODE_ID` (auto-detected via `info.getNodeID` if unset).
**Cron:** every 5 minutes.

### `scripts/gen-evidence.sh`
**Purpose:** Build a machine-readable evidence manifest (`public/api/evidence.json`) for institutional readers. Combines live state from `validator.json` with static project commitments and public-page URLs into a single self-describing JSON.
**Dependencies:** `jq`. Reads `public/api/validator.json`.
**Cron:** daily.

### `scripts/check-cron-file.sh`
**Purpose:** Pre-flight linter for `/etc/cron.d/metal-*` files. Validates the rules at `docs/CRON_CONVENTIONS.md` so a non-compliant cron file is caught before installation rather than at the next firing. Refuses files that would repeat the 2026-06-19 01:30 UTC `metal-evidence` failure (redirect to `/var/log/...` that the `deploy` user cannot create).
**Dependencies:** `bash`, `grep`. `grep -P` (perl-compatible) for the bare-`%` check; falls back to a manual-review note if absent.
**Cron:** never — invoked manually by the operator before installing or editing any `/etc/cron.d/metal-*` file.

### `scripts/gen-cycle-history.sh`
**Purpose:** Emit a per-cycle audit packet (`public/api/cycle-history.jsonl`) — one closed cycle per JSON line, joining `uptime-cycles.json` with `incidents.json` so each cycle is paired with the incident IDs that fell inside its window. Deterministic and idempotent.
**Dependencies:** `jq`. Reads `public/api/uptime-cycles.json` + `public/api/incidents.json`.
**Cron:** daily (gated on the evidence-feed cron stability check before activation).

### `scripts/peer-validators.sh`
**Purpose:** Snapshot the full Metal validator set from your local `metalgo`'s P-Chain RPC into `public/api/peers.json`. Includes per-validator rows + network summary + operator clusters (NodeIDs sharing the same reward owner).
**Dependencies:** `curl`, `jq`.
**Env:** `METALGO_API`, `OUT` (default `public/api/peers.json`), `OWN_NODE_ID`.
**Cron:** daily (output ~100 KB).

### `scripts/peer-analytics.py`
**Purpose:** Companion to `peer-validators.sh`. Reads the fresh `peers.json` and emits derived artefacts: validator-set diff vs. last snapshot (`peers-changes.json`), Gini + top-10 + Metallicus share time-series preview (`peers-gini.json`) + full history mirror (`peers-gini-history.jsonl`), fee-market percentiles (`fee-market.json` + `fee-market-history.jsonl`).
**Dependencies:** Python 3.8+, no external libraries.
**Env:** `PEERS_JSON`, `NAMES_JSON`, `STATE_DIR`, `OUT_CHANGES`, `OUT_GINI`, `GEN_ISO` (all set by the shell wrapper that invokes it).
**Cron:** daily, right after `peer-validators.sh`.

### `scripts/check-field-contracts.py`
**Purpose:** Static writer/reader field-name contract checker for every JSON/JSONL artifact the repo produces. For each feed it unions a *writer vocabulary* (example file + checked-in live file + JSON schema + the emitting script's own object construction) and compares it against every key any consumer asks for — jq paths in shell scripts and property accesses in the site's browser JS. Reports each reader key that matches no writer name.
Exists because that mismatch is silent in both languages: jq yields `null` and JS yields `undefined` for a key nobody ever wrote, so a wrong name renders as a blank field rather than an error. On 2026-08-04 a reader asking for `.dag_root` against a writer emitting `dag_root_hash` nulled `prev_anchor_root` and would have severed the on-chain hash chain; the same class was found live in `gen-cycle-history.sh` and `incidents.js` on 2026-08-06.
Severity model: **CRITICAL** = unknown key guarded by a default (`// ""`, `|| "—"`) — silently substitutes forever; **HIGH** = unguarded, fails loudly downstream; **LOW** = dead fallback alternative. Feeds and readers are discovered automatically (no hand-kept list to rot); `--coverage` prints per-artifact vocabulary sources and every read site that was skipped, so blind spots stay visible.
**Dependencies:** Python 3 stdlib only. No network, no repo writes, read-only.
**Usage:** `python3 scripts/check-field-contracts.py [--coverage] [--strict] [--json] [--include-tests]`
**Waivers:** `tests/field-contracts/waivers.txt` — `<artifact> <key>  # reason`; a waiver without a justification is itself an error (exit 2).
**Cron:** none — CI gate via `tests/field-contracts/test-field-contracts.sh`, discovered by `tests/run-all-tests.sh` and therefore run by both `validate.yml` and `ci-main.yml`.

### `scripts/check-validator.sh`
**Purpose:** Lightweight liveness check of your validator's presence in `getCurrentValidators` and its uptime number. Returns non-zero if your NodeID is missing or uptime drops below threshold. Suitable for cron or external monitoring.
**Dependencies:** `curl`, `jq`.
**Env:** `METALGO_API`, `OWN_NODE_ID`, `UPTIME_THRESHOLD` (default 80).

### `scripts/check-validator-period.sh`
**Purpose:** Daily check of the remaining time on your current validation period. Logs to a configurable log path. Pair with `notify.sh` for T-14/7/3/1 day alerts.
**Dependencies:** `curl`, `jq`.

### `scripts/uptime-history.sh`
**Purpose:** Long-horizon uptime track record. Appends daily samples to a master JSONL (keep-forever). Regenerates `public/api/uptime-recent.json` (last 90 days) and `public/api/uptime-cycles.json` (per-cycle summaries on cycle close, with `period_id` + explorer URL for audit).
**Dependencies:** `curl`, `jq`. Read-only access to `validator.json`.
**Cron:** daily, off-peak.

### `scripts/node-health-daily.sh`
**Purpose:** Once-a-day snapshot of host + `metalgo` health, kept forever. Master JSONL on the validator host (includes load, CPU, RAM, disk for ops use), plus a sanitized public subset (peer count, chain heights, bootstrap, container status) suitable for publication.
**Dependencies:** `curl`, `jq`, `docker` (for container status).

### `scripts/gen-renewal-ics.sh`
**Purpose:** Generate an iCalendar feed of your upcoming renewal events from `validator.json`'s `endTime`. Subscribable in Google / Apple Calendar. Token-named file URL provides obscurity without auth.
**Dependencies:** `jq`. Optional: a stable random token in a configurable token file.
**Cron:** daily, regenerate so it stays current.

### `scripts/dr-drill.sh`
**Purpose:** End-to-end disaster-recovery exercise on a Mac (or any second host). Decrypts the encrypted staker key backup, verifies SHA-256, boots a local-network `metalgo` with the restored keys, confirms `info.getNodeID` matches your production NodeID, cleans up. **Never** connects to mainnet — uses `--network-id=local`. Safe to run while production is up.
**Dependencies:** `docker`, `openssl`, `tar`, `sha256sum`, `jq`.
**Frequency:** periodically; untested backups aren't backups.

---

## Tier 2 — Reusable with assumptions

Useful but assume ntfy.sh push notifications and a few env conventions. Adapt the bits that don't fit.

### `scripts/notify.sh`
**Purpose:** Push a single ntfy.sh notification. Reads topic from a file (treated as a shared secret — anyone who knows it can publish/subscribe). Used by every alert pipeline below.
**Dependencies:** `curl`.
**Env:** `NTFY_TOPIC_FILE` (path to a file holding the topic identifier — set this), `NTFY_SERVER` (default `https://ntfy.sh`), `NTFY_TAGS` (non-empty value overrides the priority-derived Tags emoji for that one notification, e.g. `tada`; unset/empty keeps the default).
**Usage:** `bash notify.sh <priority> <title> <message...>`.
**Adapt:** If you don't use ntfy, swap with your own push (Slack webhook, Telegram bot, Pushover etc.) — the calling scripts just exec this one wrapper.

### `scripts/daily-status.sh`
**Purpose:** Daily digest composer. Pushes a single notification at one of three slots (`morning` / `evening` / `night`) summarizing validator + host state.
**Dependencies:** `notify.sh`, `jq`, optionally `public/api/server-status.json` for host metrics.
**Env:** `METALGO_RPC`, `WALLET_CONFIG` (optional — for wallet balance push, see below).

### `scripts/check-anomalies.sh`
**Purpose:** Anomaly detector. Runs frequently via cron. Compares current state vs. a state file to dedup notifications. Detection rules: container status, disk > 85%, memory > 95%, peer count < 10, validator missing from `getCurrentValidators`, period T-7 / T-3 / T-1, stale public `validator.json` push.
**Dependencies:** `notify.sh`, `jq`, `curl`. State file in a configurable path.

### `scripts/peer-geo.py`
**Purpose:** Resolve peer IPs from `info.peers` to country/city via `ip-api.com` (free tier, HTTP for non-authenticated calls). Writes `public/api/peer-geo.json` (NodeID + lat/lng + country/city, no IPs exposed). Used by the world map on the home page.
**Dependencies:** Python 3.8+, no external libraries. The free ip-api endpoint has a per-IP rate limit — TTL-cache aggressively. Our implementation caches 30 min per IP.
**Cron:** every ~30 minutes.

---

## Tier 3 — Operator-specific (reference only)

These reflect our specific two-host topology and our specific initial VPS setup. Useful as patterns; not turnkey.

| Script | What it does | Why it's specific |
|---|---|---|
| `scripts/push-to-web-host.sh` | SSH push of public JSON files from validator host → web host. Flat allowlisted filenames, plus two subdirectory prefixes: `archive/anchor-{source,receipt}-<64hex>.json` (R18 per-anchor archives) and `peers-history/peers-YYYY-MM-DD.json.gz` (daily snapshots). | Tied to our forced-command SSH wrapper and web-host path layout |
| `scripts/deploy/receive-subdir-allowlist.snippet.sh` | Canonical text of the web-host-side receive block for those two subdirectory prefixes (validates the basename, creates the subdir, atomic write, content-type check). Not run directly — embedded by the installer below. | Mirrors the sender allowlist; both sides enforce it independently |
| `scripts/sync-to-validator-host.sh` | local Mac → validator host sync of `scripts/` + cron files | Specific to our deploy authorization model |
| `scripts/server-status.sh` | Host metrics JSON for ops dashboard | Tied to our `/ops/` page schema |
| `scripts/vps-bootstrap.sh` | Initial VPS setup (sshd hardening, fail2ban, docker, cron seeding) | Specific to our chosen base image |
| `scripts/security-audit.sh` | Periodic security posture check | Specific to our threat model |

---

## Tier 4 — Anchor + cycle-gate subsystem

The cryptographic-evidence anchor pipeline (identity → anchor-source → A-chain inscription → receipt) and the cycle-transition gate. Under Mac-only signing the host composes + detects; the operator's Mac signs + broadcasts; the host publishes the receipt. See [`docs/audits/constitution-2026-07-04-design-stocktake.md`](docs/audits/constitution-2026-07-04-design-stocktake.md) for the subsystem's design debt + target design.

**Anchor pipeline (v2, HC-single 4-action `fya<S>c<N>` pack):**

| Script | Purpose | Cron |
|---|---|---|
| `scripts/gen-anchor-source.sh` | Compose `public/api/anchor-source.json` (3-branch identity‖observations‖artifacts + `dag_root_computed`) from live sources. Single source of truth for the inscribed value. | on demand |
| `scripts/operator-local/commit-anchor-source.sh` | Operator-local (Mac-only) git-transfer hop: SSH-fetch the host-composed `anchor-source.json`, validate (schema + `--expect-cycle=<N+1>` match against `observations_branch.cycle_number_observed` + genesis guard), show a diff summary, and single-purpose `git commit` (optional `--push`). No key material read, no broadcast. See `docs/CYCLE_GATE.md` step 6 / `docs/VALIDATOR_RENEWAL.md` ⑥. | manual |
| `scripts/sign-anchor-event.sh` | Compose the 4-action tx from anchor-source and broadcast via `bin/safe-broadcast` (mainnet 4-gate). `--dry-run` composes + emits tx JSON only. `--output=<file>` additionally persists the emitted JSON fragment to disk — applied by default even without the flag, to `${FY_SIGN_OUTPUT_DIR:-/tmp}/fya-testnet-sign-output.json` or `fya-mainnet-sign-output.json` depending on `--chain`; a write failure there is a stderr warning only, never a failing exit. `FY_SIGN_OUTPUT_DIR` overrides the default dir (tests MUST set it to a mktemp dir — the real default path is a live operator artifact, never a throwaway). Runs on the Mac (holds the anchor key). | manual |
| `scripts/gen-anchor-receipt.sh` | Independently re-fetch the tx from mainnet Hyperion, run the 7 verify gates, write `public/api/anchor-receipt.json`. | post-broadcast |
| `scripts/append-anchor-history.sh` | Append the v2 receipt as one JSONL line to `public/api/anchor-history.jsonl` with append-only invariants (prev-tx chain, genesis prev=null). **Side effect (2026-08-06):** after a successful append, also pushes the two R18 per-anchor archive files (`archive/anchor-source-<dag_root>.json`, `archive/anchor-receipt-<tx_id>.json`) to the web host via `push-to-web-host.sh` — best-effort, never fails the append itself. Env overrides: `FYD_PUSH_TO_WEB_HOST` (push script path), `ANCHOR_ARCHIVE_DIR` (where THIS script looks to confirm a local archive file exists — does not redirect what `push-to-web-host.sh` itself reads), `FYD_NOTIFY` (failure-alert notifier), `FYD_PUBLISH_ARCHIVES=0` (kill switch — skip the publish step entirely). **`FY_LIVE=1` is required before the push or the failure alert leaves the machine** (C3 rollout 2026-08-06) — a real cycle transition must set it, e.g. `FY_LIVE=1 bash scripts/append-anchor-history.sh …`. The APPEND itself is deliberately NOT gated: a forgotten opt-in must never cost a line in an append-only ledger derived from a one-shot receipt, so without `FY_LIVE=1` the line is still written and only the two pushes are announced-and-skipped (`DEFERRED: R18 publish …`, with the exact manual push command printed). This reverses the previous instruction here: a test no longer has to stub `FYD_PUSH_TO_WEB_HOST` / `FYD_NOTIFY` to stay safe — an unstubbed test is inert by construction. Stub them only when the test's subject IS the delegate call (and then set `FY_LIVE=1` deliberately, or the stub will never be reached). | post-broadcast |
| `scripts/run-anchor-pipeline.sh` | v2 orchestrator: gen-anchor-source → sign → receipt → history. **Cannot complete as a single invocation**: composing + receipt run on the validator host, but signing is Mac-only (`sign-anchor-event.sh` exit 7 anywhere else), so in practice the four steps run manually, split across host and Mac, with a git commit/push/deploy hop for `anchor-source.json` in between (`scripts/operator-local/commit-anchor-source.sh`) — see `docs/CYCLE_GATE.md` "Separation from the anchor pipeline" / stock-take #5. | manual |
| `scripts/watch-anchor-events.sh` | Poll metalgo for validator-presence transitions; dispatch to `ANCHOR_DRIVER` (default + production: the alert-only `notify-anchor-transition.sh`). | every 5 min |
| `scripts/notify-anchor-transition.sh` | DETECTION/ALERT-ONLY driver: on a transition, ntfy-push "run the manual anchor from the Mac". Broadcasts nothing. | via watcher |
| `scripts/preview-cycle-anchor-broadcast.sh` | STAGE-1 mainnet preview, run **on the Mac** after the anchor-source commit (the §3.5 guard refuses a `sudo -u deploy` host invocation, exit 8). **Composes nothing**: uses `--source` verbatim and verifies it is byte-for-byte `git show HEAD:public/api/anchor-source.json` (exit 9 otherwise; `--allow-uncommitted` overrides, off-cycle only), then fetches the LIVE public `anchor-source.json` (cache-busted) and refuses (exit 10) if it does not yet match — push/deploy must land first (`--skip-published-check` overrides, offline/degraded use only). Produces the `--chain=mainnet-a` dry-run log that feeds safe-broadcast's gate 4, displays the exact tx shape, prints the STAGE-2 command. **Broadcasts nothing.** Cycle-generic (derives the cycle number from `--source`; renamed from `preview-cycle3-anchor-broadcast.sh`). | manual |
| `scripts/check-anchor-publish-health.sh` | Verify `anchor-source.json` is served publicly AND content-correct (`dag_root_computed` matches the on-chain anchored root recorded in `anchor-receipt.json`). Alert-only — no re-push/recover (git-deploy makes host-side recovery structurally impossible); fires a high-priority ntfy push on every failure exit, with signature+time-window dedup on repeat content-mismatch alerts (default 6h) so the normal transition-window staleness doesn't page repeatedly. | every 15 min |
| `scripts/check-pulsevm-upstream.sh` | Watch the PulseVM upstream (the VM its docs describe as A-Chain's coming foundation) for the four changes that would oblige this project to act: **T1** the "third-party node sync is not yet supported" notice disappearing from `pulsevm.dev/network/endpoints`, **T2** a mainnet section or a chain id not seen before, **T3** a new page in `llms.txt`, **T4** A-Chain Alpine's head block advancing past a threshold (default 1000/day). Reads GitHub releases, two docs pages, and Hyperion `/v2/health` — **never a chain RPC**: the Alpine endpoint is an AvalancheGo per-blockchain alias path that `broadcast-guard.sh` blocks unconditionally, and the test suite asserts the literal is absent from the script. Silent in steady state; a new release tag is logged, never pushed. **Two channels**: `high` for T1 / a mainnet section / the first-run baseline (act now), `default` for a chain-id-only T2, T3 and T4 (read when you get to it) — the endpoints page warns Alpine resets are frequent during hardening and each changes the chain id, so that path must not share T1's channel; a mixed run escalates to `high`. Every alert body names the obligation, not just the observation. Fail-open: a missing/corrupt state file fires once (reporting T3/T4 as DEFERRED rather than claiming they fired). A source stuck unreachable pages once at the third consecutive run, not daily. Exit 0 no change / 3 trigger / 4 unreachable / 5 unparseable. | daily 02:00 UTC |
| `scripts/check-identity-pins.sh` | TWO independent checks on the operator-SIGNED `public/api/identity.json`. **Check 1 (mismatch)**: does a pinned `sha256` still match the real bytes? `--mode=repo` (CI gate, wired into `validate.yml` + `ci-main.yml`) compares against the repo's own files; `--mode=live` (daily cron) compares against the served URLs. Classifies each pin from `deploy/feed-excludes.txt`: a **push-owned feed** pin (evidence / validator / uptime-cycles) drifts by construction and is only recorded, a **git-tracked** pin can only break because a commit broke it, so it alerts and fails. `deploy/identity-pin-baseline.json` acknowledges the pins already broken on 2026-08-06, scoped to the exact (pinned, observed) pair so any further edit is still red. **Check 2 (kind gate, exit 6, added 2026-08-17)**: may this pin exist AT ALL, per `deploy/publication.json`'s `kind`? `kind=stream` MUST NOT be pinned unless acknowledged in the registry's `known_kind_violations`; a row with no `kind` in {stream,static,record} (typo, or undeclared kind) is also refused, never silently allowed. `kind` is a separate axis from `deploy/feed-excludes.txt` (e.g. `api/archive/` is push-owned AND `kind=record`, safe to pin). Alert-only; the repair is operator-manual because CI has no access to the signing key — see `docs/OPERATOR_IDENTITY_SETUP.md` § "Repairing a broken artifact pin" for both checks' remedies. **Acknowledgement expiry (2026-08-17)**: both acknowledgement lists are told when they go stale — `OBSOLETE-BASELINE` for `deploy/identity-pin-baseline.json`, `OBSOLETE-KIND-ACK` for the registry's `known_kind_violations` (the pin is gone, the entry's declared path is not what the manifest pins, or the target is no longer `kind=stream` — the same three conditions `tests/publication-registry/`'s T7 applies). Both are **report-only**: they never change the exit code and never push, because an expired acknowledgement is housekeeping rather than breakage and T7 is the gate that fails CI. What this adds is the cron path, which had no expiry report at all and would otherwise have kept printing `KIND-BASELINED` for entries that mute nothing. | daily 01:00 UTC (live) + every CI run (repo) |
| `scripts/install-metal-identity-pins-cron.sh` | Install `/etc/cron.d/metal-identity-pins` (daily 01:00 UTC = 10:00 JST, `--mode=live`). Root-only unless `FYD_CRON_TARGET` overrides (harness mode); pre-flight-linted by `check-cron-file.sh`. | manual (operator) |
| `scripts/run-testnet-rehearsal.sh` | Operator installer for the testnet anchor rehearsal (gate-1 material). | manual |
| `scripts/install-rehearsal-preflight.sh` | READ-ONLY, fail-closed pre-flight for the row above — the one command that answers "would the rehearsal fail today, and why", days before the operator is called. Installs nothing, writes nothing, has no broadcast pathway, and reaches the keystore only through the non-destructive `key:list`. Nine checks: tools (incl. the bare name `timeout`, whose absence turns a locked keystore into an indefinite hang), §3.5 separation with `HOME` hard-pinned to the TESTNET keystore, the rehearsal + mainnet config dirs, the identity key pair, `anchor-source.json` worktree==committed==published (the precursors of `preview-cycle-anchor-broadcast.sh`'s exits 9/10), the published ledger vs `--expect-cycle`, the published `anchor-history.jsonl` vs `--expect-cycle` (the second observation that separates a DRESS run from the DAY-OF run — the cycle-history ledger structurally cannot), and the CURRENT on-chain `<actor>@anchor` key's presence in the testnet keystore (the 2026-07-31 failure class). Check 9 also carries the rehearsal's SECOND lock probe (a read-only `proton account <actor>`, step 4/10), because a keystore can list keys and still refuse a signing-adjacent read; `proton chain:set` is deliberately NOT carried, since it writes proton-cli's config. Every check is evaluated and reported; the exit is the FIRST failure's code (0 green / 1 usage / 2 tool / 3 config / 4 key absent / 5 anchor-source bytes / 6 cycle numbering / 7 keystore unusable for signing = inconclusive / 8 §3.5 / 9 unobservable). No FLAG bypasses a check and a locked keystore is never reported as a missing key; the `FYP_*` env overrides CAN redirect any observation to a local file (that is how the suite runs hermetically), so any run with one set is labelled **NON-AUTHORITATIVE** in the banner, the summary and the verdict — an unqualified green is only printable with no override in effect. **`--expect-cycle` here takes `run-testnet-rehearsal.sh`'s meaning (the cycle being INSCRIBED), which is one MORE than `cycle-transition.sh --expect-cycle` (the cycle CLOSING today). The paired value is printed ONCE, by check 8, after the run is classified — it is `M-1` only on the day-of run; on a dress run no cycle closes today and the answer is N/A.** Do not paste a green transcript into this repo: it names the rehearsal actor and 16 chars of the on-chain anchor pubkey. | manual (before every rehearsal) |

**Cycle-gate (transition safety):**

| Script | Purpose | Cron |
|---|---|---|
| `scripts/cycle-gate.sh` | Passive gate consulted before cycle-dependent side effects. `broadcast` is signature-gated; `observe` + `cycle-artifact-write` are always green (recording a closed cycle is backward-looking). | via consumers |
| `scripts/resume-after-cycle-start.sh` | Active operator command run after a new cycle starts (v2 = 3 phase: Phase 1 verify chain + anchor-source freshness + identity signature, Phase 2 atomic state write, Phase 3 report). **No broadcast** — the anchor pipeline is a separate, Mac-signed step (see `scripts/run-anchor-pipeline.sh` row above). | manual |
| `scripts/cycle-transition.sh` | Cycle-transition orchestrator. Two read-only modes. **`--print-only`**: prints the day's 14 execution units across 6 phases with every value resolved, each command tagged `# host` / `# Mac`, and the 4 operator stop points marked; needs `--expect-cycle=<N>` (the cycle closing today) and `--ledger=<path>`, where the ledger CHECKS that declaration rather than supplying it. **`--status`**: re-measures the 5 post-conditions of design-doc §5 against the PUBLISHED artifacts (phase 4 has none and is reported as such, not invented), and refuses with exit 71 when `--expect-cycle` is contradicted by the on-chain period or the cycle boundary — the check `--print-only` cannot make. Exits 0 / 69 (something incomplete) / 70 (something unobservable — never resolved to complete). **Executes nothing** — there is no execution code path in the file, no mode ssh's, pushes, notifies or writes outside a scratch dir it removes, and `tests/orchestrator-guard/` permanently forbids a broadcast-tool string in it. `docs/CYCLE_GATE.md` stays canonical and `tests/cycle-transition/` fails if the two drift. | manual |

**Security guards + one-shot installers:**

| Script | Purpose | Cron |
|---|---|---|
| `scripts/broadcast-guard.sh` | PreToolUse hook enforcing the PRIME DIRECTIVE (blocks raw broadcast commands without the safe-broadcast marker + fresh operator token). | hook |
| `scripts/publish-guard.sh` | Block forbidden host identifiers + operator PII from entering public content (6-layer detector + 3-layer hook). | hook/pre-commit |
| `scripts/lib/publish-scan.sh` | Send-time layer over `publish-guard.sh` for content that never touches git — `push-to-web-host.sh`'s targets (`public/api/*.json`, `archive/`, `peers-history/`, `public/calendar/`) are gitignored by design, and `sync-to-validator-host.sh` rsyncs `scripts/` straight onto the host, so neither is seen by the git-hook-based guard above. Scans the OUTBOUND BYTES THEMSELVES right before they leave the machine and refuses the transfer on any non-zero result — no path allowlist, no trust in a prior git-committed version. `fyd_publish_scan_selftest` proves the guard still detects on THIS machine (a known-bad probe payload) before the first send of a process; `fyd_publish_scan_file <path> [label]` scans one file's exact bytes. | library (sourced) |
| `scripts/sanitize-history.sh` | One-command scrub of forbidden host identifiers + PII across all git history (filter-repo, bundle backup). | manual |
| `scripts/install-tier1-hook.sh` | Install the tier-1 PreToolUse hooks (broadcast-guard + publish-guard); merges into an existing `.claude/settings.json` rather than clobbering it. | manual |
| `scripts/install-git-hooks.sh` | Idempotently set this clone's `core.hooksPath=.githooks` (pre-commit secret-scan + pre-push publish-guard); `--check` verifies without mutating. | manual |
| `scripts/setup-schema-validator.sh` | Ensure a JSON-schema validator (`ajv` or `python3`+`jsonschema`) is on `PATH` for the anchor pipeline's mandatory schema-validation gate (R13), installing via `npm`/`pip3` only — never touches a system binary, symlink, or interpreter directly. See [`docs/SCHEMA_VALIDATOR_CONVENTIONS.md`](docs/SCHEMA_VALIDATOR_CONVENTIONS.md). | manual |
| `scripts/apply-external-link-attrs.sh` | Normalize external-link attributes across `public/`: every external `<a>` gets `target="_blank"` and a `rel` that unions in `noopener noreferrer` plus `nofollow` unless the host is on the verification allowlist. `--check` mode writes nothing and is the non-writing compliance gate CI runs in both `ci-main.yml` and `validate.yml`. See [`docs/superpowers/specs/2026-07-08-external-link-attrs-design.md`](docs/superpowers/specs/2026-07-08-external-link-attrs-design.md). | manual / CI (ci-main.yml, validate.yml) |
| `scripts/install-anchor-watch-alert-only.sh` | Re-enable the anchor-watch cron in alert-only mode (`ANCHOR_DRIVER=notify-anchor-transition.sh`). | manual |
| `scripts/install-repoint-publish-crons.sh` | Repoint the live publish crons from the retired `push-to-xserver.sh` to the canonical `push-to-web-host.sh` (superset-checked, backed up, `--dry-run`/`--purge-orphans`). | manual |
| `scripts/install-metal-anchor-publish-health-cron.sh` | Install the `/etc/cron.d/metal-anchor-publish-health` cron. | manual |
| `scripts/install-metal-pulsevm-watch-cron.sh` | Install `/etc/cron.d/metal-pulsevm-watch` (daily 02:00 UTC = 11:00 JST). Root-only unless `FYD_CRON_TARGET` is overridden; lint-checked pre-flight, idempotent, backs up a differing prior target. | manual |
| `scripts/install-cron-env-headers.sh` | Add the missing `SHELL=/bin/bash` + `PATH` env headers to `/etc/cron.d/metal-*` files (linter rule 5; idempotent, backed up, `--dry-run`). | manual |
| `scripts/install-cron-audit-markers.sh` | Sibling remediation script (2026-08-06, H2): add the missing `{ ... }` brace-wrap + start/end markers + `rc=$?` capture (linter rules 2/3) to `/etc/cron.d/metal-*`/`freedom-yield-*` files — same idempotent/backed-up/`--dry-run`/sidecar-skip design as `install-cron-env-headers.sh`. Only rewrites the unambiguous shape (exactly one `>>`, no pre-existing brace/echo/rc); anything else is left untouched and the whole file is flagged "needs operator review". Never touches header lines or the redirect target. | manual |
| `scripts/audit-cron-dir.sh` | Read-only: runs `check-cron-file.sh` against every `metal-*`/`freedom-yield-*` file in a directory (default `/etc/cron.d`) and asserts all are lint-clean in one command — the "is lint exit 0 usable as a signal, right now" check for after a remediation pass. | manual |
| `scripts/lib/cron-filename-guard.sh` | Shared `is_cron_executed_filename()` — the crontab(5)/run-parts(8) filename-executability guard, reused (not re-implemented) by `install-cron-audit-markers.sh` and `audit-cron-dir.sh`. | library (sourced) |
| `scripts/advance-host-checkout.sh` | Self-heal: FF-only advance the validator-host git checkout to `origin/main` (refuses + alerts high if the host is ever ahead — a host must never author). Discards `public/` deploy cache-bust dirt before pulling, EXCEPT `public/api/anchor-source.json`: real (non-cache-bust) dirt there is durably preserved to `.anchor-source-preserve/` (outside `public/`, survives the next deploy's `public/` rsync --delete) before the discard runs, then resolved once the pull's effect on that path is known (still-pending / self-healed / diverged — see [`docs/HOST_CHECKOUT_AUTO_ADVANCE.md`](docs/HOST_CHECKOUT_AUTO_ADVANCE.md)). Then `self_heal_lossless_dirt` absorbs any other unstaged/untracked dirt that is byte-identical (content+mode) to `origin/main` — the rsync-clobber signature — firing a single batched default-priority ntfy notify (`host-advance: self-healed N file(s)`) if one or more files were absorbed this run; anything not byte-identical still hits git's own FF-pull refusal. As of the 2026-07-13 delivery-ownership inversion this is the sole delivery path for every git-tracked file outside `public/`. Primary defense against host `HEAD` drift; see [`docs/HOST_CHECKOUT_AUTO_ADVANCE.md`](docs/HOST_CHECKOUT_AUTO_ADVANCE.md). | cron (daily, 04:45 UTC) + deploy time (piped over SSH from `deploy.yml`'s "Advance host checkout to origin/main" step, before the `public/` rsync) |
| `scripts/install-metal-host-advance-cron.sh` | Install the `/etc/cron.d/metal-host-advance` cron (lint-checked, idempotent), scheduled 30 minutes before the drift backstop below. | manual |
| `scripts/check-host-drift.sh` | Daily backstop: alert (ntfy) if the validator-host checkout still diverges from origin/main (local commits, code-zone edits, far behind) after `advance-host-checkout.sh` should already have self-healed it — fires only when that self-heal has stopped working. Read-only; never pulls. | cron (daily, 05:15 UTC) |
| `scripts/install-metal-host-drift-cron.sh` | Install the `/etc/cron.d/metal-host-drift` cron (lint-checked, idempotent). | manual |
| `scripts/check-scripts-freshness.sh` | Read-only fail-closed freshness gate: fetches `origin/main` and reports whether local HEAD is behind (exit 0 fresh / 1 stale / 3 fetch-failed). Library-style — never alerts or pulls; callers (e.g. the anchor pipeline) own the alerting/fail-closed decision. | library (invoked by other scripts) |
| `scripts/check-watch-validators.sh` | Monitor a private host-local list of watched validators for state changes (name reveal / first delegation / departure / return); one batched min/low-priority ntfy per run, explorer sanity gate against fabricated mass departures. | cron (JST daytime) |
| `scripts/install-watch-list.sh` | Compose the private `/etc/freedom-yield/watch-list.json` (from the monitor's own state file or explicit `--ids`); validated, idempotent, backed up. | manual |
| `scripts/install-watch-cron.sh` | Install `/etc/cron.d/metal-watch-validators` with the JST-daytime-only schedule (`0 0,4,8,12 * * *` UTC = 09/13/17/21 JST); idempotent, backed up, `--dry-run`. | manual |
| `scripts/install-xserver-anchor-source-allowlist.sh` | RETIRED/FLIPPED 2026-08-06: originally extended the Xserver forced-command allowlist for `anchor-source.json`; now REMOVES it (idempotent) — that file is git-deploy owned since 2026-07-07 and push-to-web-host.sh will never send it. `SKIP_SSH=1 WRAPPER_FILE=<path>` for local testing. | manual |
| `scripts/install-host-log-dir.sh` | Ensure the repo-local `logs/` directory exists on the validator host with `deploy:deploy` ownership + `755`, so cron `>> .../logs/*.log` redirects never fail after a fresh clone or `git reset --hard` (companion to the tracked `logs/.gitkeep`). Idempotent, refuses local Mac paths. | manual |
| `scripts/install-xserver-sig-allowlist.sh` | RETIRED/FLIPPED 2026-08-06: originally extended the Xserver receive-metal-push allowlist for `anchor-source.json.sig` / `anchor-receipt.json.sig` / `identity.json.sig`; now REMOVES all three (idempotent) — the first two have no producer anywhere in the repo, the third is git-deploy owned and never push-sent. `SKIP_SSH=1 WRAPPER_FILE=<path>` for local testing. | manual |
| `scripts/install-xserver-subdir-allowlist.sh` | Teach the Xserver receive-metal-push wrapper the two subdirectory prefixes (`archive/`, `peers-history/`) by embedding `scripts/deploy/receive-subdir-allowlist.snippet.sh` at its top. Auto-detects the destination api dir (refuses on ambiguity — `FY_WEB_API_DIR=` to override), backs up, syntax-checks before installing, idempotent, `--dry-run` / `--print-remote`. | manual |
| `scripts/deploy/build-rsync-excludes.sh` | Emit the shared rsync feed-exclusion args (single source of truth: `deploy/feed-excludes.txt`) for both deploy targets, so validator-host and Xserver rsyncs cannot drift. | CI (deploy.yml) |
| `scripts/deploy/render-publication.sh` | Render (`<artifact>`), extract (`--extract`) or diff (`--check`) the regions derived from `deploy/publication.json`: `feed-excludes`, `gitignore-block`, `push-allowlist-doc`, `push-allowlist-case`. **Prints to stdout only — it never edits a file, and as of C1 Phase 0 no deploy or push path consumes its output.** The three target files are still hand-maintained and still what actually runs; the registry is currently a detector (drift turns `tests/publication-registry/` red) and not an enforcement. `--as-built` applies the registry's declared `known_render_drift` so a known, expiring difference is not confused with a new one. | CI (via `tests/run-all-tests.sh`) |
| `scripts/install-xserver-static-deploy-key.sh` | Install the `rrsync -wo`-restricted `authorized_keys` entry so GitHub Actions can rsync `public/` to the Xserver public origin, confined to the metal public dir on the shared host. | manual |
| `scripts/install-delegator-feed-started-at.sh` | One-line installer for the delegator-feed `started_at` field. | manual |
| `scripts/notify-evidence-health.sh` | Health check for `/api/evidence.json` publication; `--summary` mode for the daily digest. | daily |
| `scripts/anomaly-state-init.sh` | Operator-driven baseline state initialiser for the anomaly detector. | manual |

---

## Cron pattern we use

One file per logical pipeline, kept readable. Roughly:

```cron
# REQUIRED since 2026-08-06 (scripts/lib/side-effects.sh, C3 rollout): every
# production side effect — ntfy notify, web-host push, /var/lib/freedom-yield
# state write, public feed write — is opt-in behind FY_LIVE=1. Without this
# line each entry below still runs and still returns its usual exit code, but
# sends and writes nothing: it emits one "DRY: would …" line per suppressed
# effect instead. scripts/check-cron-file.sh Rule 6 fails a cron file that
# references a side-effecting script without it, and
# scripts/install-cron-env-headers.sh adds it to an already-installed file.
#
# THOSE "DRY: would …" LINES GO TO stderr, so they are only visible if the
# entry redirects. Every entry below therefore ends in `2>&1 | logger -t <tag>`.
# Drop it and a tick that lost FY_LIVE=1 becomes SILENT — cron mails the output
# to a host that usually has no MTA, and the loudness this whole design depends
# on is thrown away. Read the result with `journalctl -t <tag>`.
#
# A CHAIN MUST BE WRAPPED IN `{ … ; }` FOR THAT REDIRECT TO MEAN ANYTHING.
# A pipeline binds TIGHTER than `&&`, so
#     A && B && C 2>&1 | logger        parses as   A && B && ( C 2>&1 | logger )
# and only the LAST command's output ever reaches the log; A's and B's go to
# MAILTO and are dropped. Measured on /bin/sh and /bin/bash: of six output
# streams from a three-command chain, two reach the tag and four are lost.
# `{ A && B && C ; } 2>&1 | logger` captures all six. This is not a style
# preference — scripts/check-cron-file.sh Rule 2 fails a `&&` chain whose
# redirect OR pipe covers only the last command (widened 2026-08-07; the
# original rule only inspected lines containing `>>`, so this exact pipe
# shape was invisible to it), and scripts/install-watch-cron.sh already
# generates the brace form. A single `bash -c "A && B && C" 2>&1 | logger`
# invocation also passes Rule 2 — the chain is then opaque to cron and the
# outer pipe correctly scopes the one bash -c process (see the
# freedom-yield-peer-geo cron, this repo's one real example of that shape).
FY_LIVE=1

# Push to web frequently
*/5 * * * * root { /path/to/scripts/node-info.sh && /path/to/scripts/push-to-web-host.sh ; } 2>&1 | logger -t node-info

# Anomaly detection frequently
*/5 * * * * root /path/to/scripts/check-anomalies.sh 2>&1 | logger -t check-anomalies

# Daily status digests at multiple times per day
# (operator's chosen times)

# Validator-set snapshot + analytics + history (daily)
0 4 * * * root { /path/to/scripts/peer-validators.sh && /path/to/scripts/peer-analytics.py && /path/to/scripts/push-to-web-host.sh ; } 2>&1 | logger -t peer-validators

# Uptime + node-health history (daily, off-peak)
30 0 * * * root /path/to/scripts/uptime-history.sh 2>&1 | logger -t uptime-history
35 0 * * * root /path/to/scripts/node-health-daily.sh 2>&1 | logger -t node-health

# Renewal calendar (rebuild daily, stays current)
10 0 * * * root { /path/to/scripts/gen-renewal-ics.sh && /path/to/scripts/push-to-web-host.sh ; } 2>&1 | logger -t renewal-ics
```

The exact times are mostly arbitrary; spread them out so the push pipeline doesn't try to send 8 files in the same second.

---

## Dependencies summary

| Tier | Runtime | External services |
|---|---|---|
| 1 | bash, `curl`, `jq`, `bc`, python3 (for analytics), `docker` (for DR drill) | Local `metalgo` only |
| 2 | + ability to read a topic file | ntfy.sh (free tier fine), ip-api.com (free tier, TTL-cache) |
| 3 | + SSH access between two hosts | n/a |

---

## What's intentionally not here

- **Slashing protection / signer infra**. Metal Blockchain has no slashing, so we don't need Web3Signer / Dirk / Vouch. If/when slashing arrives, add it.
- **MEV smoothing**. Avalanche-family consensus doesn't expose MEV the way Ethereum builders do.
- **Liquid staking primitives**. Out of scope for a delegation-only operator.
- **Subnet validator tooling**. Will add once subnet deployments actually appear.

---

## Adapting for your own node

The bare minimum to get started:

1. Clone the repo, copy `scripts/` to your validator host. Pick an install path; edit cron accordingly.
2. Install ntfy (or replace `notify.sh` with your push of choice). Put the topic in a file and point `NTFY_TOPIC_FILE` at it.
3. Make sure `metalgo` is reachable at `http://localhost:9650` (or set `METALGO_API`).
4. Drop one of the cron lines from above to validate the pipeline before adding more.
5. The `public/api/` JSON files generated by these scripts are designed to be served by any static file server — Caddy, nginx, S3, GitHub Pages, your choice.

If you fork any of this and find a bug or have an obvious improvement, a PR or issue at this repo is welcome. We don't run a community channel; the code itself is the medium.

---

## Related

- [`docs/`](docs/) — operational runbooks (incident response, key rotation, disaster recovery, security layers)
- [`/data/`](https://metal.freedom-yield.com/data/) — every public JSON / JSONL feed served by this site, with schema and curl examples
- [`CLAUDE.md`](CLAUDE.md) — project context for AI sessions
