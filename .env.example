# 本番デプロイ時は cp .env.example .env して値を埋める

# ---- 本番(VPS)で使う ----
# 公開ドメイン
DOMAIN=metal.freedom-yield.com
# Let's Encrypt のアカウント連絡先メール(証明書失効通知などが届く)
ACME_EMAIL=

# ---- ローカル開発で使う(任意) ----
# ホスト側で 8080 が埋まっているとき変更
# HTTP_PORT=18080
